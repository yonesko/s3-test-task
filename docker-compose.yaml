services:
  gateway:
    build:
      dockerfile: gateway.Dockerfile
    ports:
      - "8361:8361"
  storage:
    deploy:
      replicas: 10
    environment:
      GATEWAY_ADDR: gateway:8361
    build:
      dockerfile: storage.Dockerfile
    depends_on:
      - gateway